---
phase: 05-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/skills/djinn/SKILL.md
  - plugin/skills/djinn-planning/SKILL.md
  - plugin/skills/djinn-planning/new-project/SKILL.md
  - plugin/skills/djinn-planning/plan-milestone/SKILL.md
  - plugin/skills/djinn-planning/discuss-milestone/SKILL.md
  - plugin/skills/djinn-planning/progress/SKILL.md
  - packages/planning/skills/djinn/SKILL.md
  - packages/planning/skills/djinn-planning/SKILL.md
autonomous: true
requirements:
  - DIST-01
  - DIST-05

must_haves:
  truths:
    - "Every workflow SKILL.md starts with a 'Before You Begin' preamble that calls system_ping() and stops with a clear error message if djinn-server is not responding"
    - "The preamble error message is exactly: 'djinn-server is not running. Start it with: `djinn-server --daemon`'"
    - "The Claude Code plugin's existing hooks.json SessionStart hook (djinn-server --ensure-daemon) is preserved and complemented by the preamble, not replaced"
    - "The packages/planning/skills/ directory is a complete mirror of plugin/skills/ containing all SKILL.md files and cookbooks ready for NPM distribution"
    - "Skill file content is identical between plugin/skills/ and packages/planning/skills/ -- no per-runtime variants"
  artifacts:
    - path: "plugin/skills/djinn-planning/new-project/SKILL.md"
      provides: "New project workflow with system_ping preamble"
      contains: "system_ping"
    - path: "plugin/skills/djinn-planning/plan-milestone/SKILL.md"
      provides: "Plan milestone workflow with system_ping preamble"
      contains: "system_ping"
    - path: "plugin/skills/djinn-planning/discuss-milestone/SKILL.md"
      provides: "Discuss milestone workflow with system_ping preamble"
      contains: "system_ping"
    - path: "plugin/skills/djinn-planning/progress/SKILL.md"
      provides: "Progress workflow with system_ping preamble"
      contains: "system_ping"
    - path: "packages/planning/skills/"
      provides: "Complete skill content bundle for NPM distribution"
      contains: "djinn-planning"
  key_links:
    - from: "plugin/skills/djinn-planning/new-project/SKILL.md"
      to: "system_ping MCP tool"
      via: "Preamble section calls system_ping before workflow steps"
      pattern: "system_ping"
    - from: "packages/planning/skills/"
      to: "plugin/skills/"
      via: "Mirror copy -- identical content for distribution"
      pattern: "SKILL.md"
    - from: "plugin/hooks/hooks.json"
      to: "djinn-server --ensure-daemon"
      via: "SessionStart hook ensures daemon is running for Claude Code"
      pattern: "ensure-daemon"
---

<objective>
Add daemon health check preambles to all workflow SKILL.md files and create the skills content bundle for the NPM package, ensuring the Claude Code plugin and NPM distribution contain identical, complete skill content.

Purpose: DIST-05 requires that workflows verify djinn-server is running before executing. Claude Code has hooks for auto-start, but all runtimes need the SKILL.md preamble as a safety net. DIST-01 requires the Claude Code plugin to include the planning skills -- this plan ensures plugin/skills/ is complete and mirrors it into packages/planning/skills/ for NPM distribution.

Output: All 6 SKILL.md files have system_ping preambles. The packages/planning/skills/ directory contains a complete copy of all skills and cookbooks ready for NPM packaging.
</objective>

<execution_context>
@/home/fernando/.claude/get-shit-done/workflows/execute-plan.md
@/home/fernando/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-distribution/05-CONTEXT.md
@.planning/phases/05-distribution/05-RESEARCH.md
@plugin/skills/djinn/SKILL.md
@plugin/skills/djinn-planning/SKILL.md
@plugin/skills/djinn-planning/new-project/SKILL.md
@plugin/skills/djinn-planning/plan-milestone/SKILL.md
@plugin/skills/djinn-planning/discuss-milestone/SKILL.md
@plugin/skills/djinn-planning/progress/SKILL.md
@plugin/hooks/hooks.json

<interfaces>
<!-- Existing SKILL.md structure: each starts with a title line, then description paragraph, then ## sections -->
<!-- The preamble must be inserted BEFORE the first workflow-specific section (after title + description) -->

<!-- USER DECISIONS (from 05-CONTEXT.md -- LOCKED and NON-NEGOTIABLE): -->
<!-- - Each SKILL.md starts with a system_ping preamble check -->
<!-- - Error message: one-liner with fix command ("djinn-server is not running. Start it with: `djinn-server --daemon`") -->
<!-- - No version compatibility check in v1 -- ping succeeds = good enough -->
<!-- - Hooks only for Claude Code (hooks.json already exists) -- other runtimes rely on SKILL.md preamble + manual daemon start -->
<!-- - Raw SKILL.md is sufficient for all runtimes -- no adapter layer or per-runtime transformation -->

<!-- CLAUDE'S DISCRETION: -->
<!-- - MCP resource URI scheme for skills and cookbooks -->

<!-- PREAMBLE PATTERN (from 05-RESEARCH.md): -->
<!-- ## Before You Begin -->
<!-- Before executing this workflow, verify djinn-server is reachable: -->
<!-- 1. Call `system_ping()` -->
<!-- 2. If it fails or times out: STOP. Tell the user: -->
<!--    > djinn-server is not running. Start it with: `djinn-server --daemon` -->
<!-- 3. If it succeeds: proceed with the workflow below. -->

<!-- HOOKS COMPLEMENT (not replace): -->
<!-- Claude Code hooks.json SessionStart -> djinn-server --ensure-daemon (auto-starts daemon) -->
<!-- The SKILL.md preamble is a SECOND layer of protection for all runtimes -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add system_ping preamble to all 6 SKILL.md files</name>
  <files>plugin/skills/djinn/SKILL.md, plugin/skills/djinn-planning/SKILL.md, plugin/skills/djinn-planning/new-project/SKILL.md, plugin/skills/djinn-planning/plan-milestone/SKILL.md, plugin/skills/djinn-planning/discuss-milestone/SKILL.md, plugin/skills/djinn-planning/progress/SKILL.md</files>
  <action>
Add a "Before You Begin" preamble section to each of the 6 SKILL.md files. The preamble verifies djinn-server connectivity before any workflow logic runs.

**Preamble text (identical for all 6 files):**
```markdown
## Before You Begin

Before executing this workflow, verify djinn-server is reachable:

1. Call `system_ping()`
2. If it fails or times out: **STOP.** Tell the user:
   > djinn-server is not running. Start it with: `djinn-server --daemon`
3. If it succeeds: proceed with the workflow below.
```

**Insertion point for each file:**

1. **plugin/skills/djinn/SKILL.md** -- Insert after the frontmatter closing `---` and the opening description paragraph (after "For project planning workflows, use the `djinn-planning` skill instead."), before `## Session Start`. The preamble goes between the intro text and the first functional section.

2. **plugin/skills/djinn-planning/SKILL.md** -- Insert after the frontmatter closing `---` and the "Route to the correct planning workflow..." line, before `## Detect Workflow`.

3. **plugin/skills/djinn-planning/new-project/SKILL.md** -- Insert after the opening description paragraph (after "...ready for milestone planning."), before `## Tools`.

4. **plugin/skills/djinn-planning/plan-milestone/SKILL.md** -- Insert after the opening description paragraph, before `## Arguments`.

5. **plugin/skills/djinn-planning/discuss-milestone/SKILL.md** -- Insert after the opening description paragraph, before `## Arguments`.

6. **plugin/skills/djinn-planning/progress/SKILL.md** -- Insert after the v2 status notice and description paragraph, before `## Arguments`.

**Rules:**
- Use the EXACT same preamble text in all 6 files (no per-file variations)
- Preserve all existing content -- this is an insertion, not a replacement
- The preamble includes `system_ping` in backticks so agents recognize it as an MCP tool call
- The error message uses exactly: "djinn-server is not running. Start it with: `djinn-server --daemon`" per locked decision
- Do NOT modify hooks.json -- it already handles SessionStart for Claude Code
- Verify the SKILL.md line count stays reasonable after insertion (the preamble adds ~7 lines to each file, well within budgets)
  </action>
  <verify>
    <automated>count=0; for f in plugin/skills/djinn/SKILL.md plugin/skills/djinn-planning/SKILL.md plugin/skills/djinn-planning/new-project/SKILL.md plugin/skills/djinn-planning/plan-milestone/SKILL.md plugin/skills/djinn-planning/discuss-milestone/SKILL.md plugin/skills/djinn-planning/progress/SKILL.md; do grep -q "system_ping" "$f" && count=$((count+1)); done; [ "$count" -eq 6 ] && echo "PASS: system_ping preamble found in all 6 SKILL.md files" || echo "FAIL: system_ping found in only $count of 6 files"</automated>
  </verify>
  <done>All 6 SKILL.md files contain the "Before You Begin" preamble with system_ping() call. The error message is "djinn-server is not running. Start it with: `djinn-server --daemon`" in all files. hooks.json is unchanged. No SKILL.md exceeds its line budget.</done>
</task>

<task type="auto">
  <name>Task 2: Create packages/planning/skills/ as a complete mirror of plugin/skills/</name>
  <files>packages/planning/skills/djinn/SKILL.md, packages/planning/skills/djinn-planning/SKILL.md</files>
  <action>
Create the `packages/planning/skills/` directory as a complete copy of `plugin/skills/`, containing all SKILL.md files and cookbook files. This is the content bundle that the NPM package distributes.

**Copy the entire plugin/skills/ tree:**
```
packages/planning/skills/
├── djinn/
│   ├── SKILL.md
│   └── cookbook/
│       ├── task-management.md
│       ├── memory-management.md
│       ├── execution-planning.md
│       └── work-decomposition.md
└── djinn-planning/
    ├── SKILL.md
    ├── cookbook/
    │   ├── planning-templates.md
    │   └── task-templates.md
    ├── new-project/
    │   └── SKILL.md
    ├── plan-milestone/
    │   └── SKILL.md
    ├── discuss-milestone/
    │   └── SKILL.md
    └── progress/
        └── SKILL.md
```

**Method:** Use a recursive copy of `plugin/skills/` to `packages/planning/skills/`. This MUST happen AFTER Task 1 (which adds the preambles), so the copied files include the system_ping preambles. Since both tasks are in the same plan, the executor runs them sequentially -- Task 2 gets the already-modified files from Task 1.

**Verification checklist:**
- Count total files in packages/planning/skills/ -- must match plugin/skills/ exactly
- Diff packages/planning/skills/ against plugin/skills/ -- must show zero differences
- The packages/planning/skills/ files include the system_ping preambles added in Task 1

**Do NOT copy:**
- plugin/.claude-plugin/ (plugin manifest -- not part of skill content)
- plugin/hooks/ (Claude Code-specific hooks -- not portable)
- plugin/.mcp.json (Claude Code plugin MCP config -- installer writes per-runtime configs separately)

The skills/ directory is the ONLY content the NPM package distributes. The installer (from Plan 01) copies this directory to the appropriate location for each runtime.
  </action>
  <verify>
    <automated>diff -rq plugin/skills/ packages/planning/skills/ 2>&1 | grep -v "^$" | wc -l | grep -q "^0$" && echo "PASS: skills directories are identical" || echo "FAIL: skills directories differ"</automated>
  </verify>
  <done>packages/planning/skills/ is an exact mirror of plugin/skills/ with all 6 SKILL.md files (including system_ping preambles), 4 djinn cookbooks, and 2 djinn-planning cookbooks. Zero differences between the two directories. The NPM package's files field includes skills/ so all content will be published.</done>
</task>

</tasks>

<verification>
1. Preamble in all workflow files: `grep -l "system_ping" plugin/skills/djinn/SKILL.md plugin/skills/djinn-planning/SKILL.md plugin/skills/djinn-planning/new-project/SKILL.md plugin/skills/djinn-planning/plan-milestone/SKILL.md plugin/skills/djinn-planning/discuss-milestone/SKILL.md plugin/skills/djinn-planning/progress/SKILL.md | wc -l` returns 6
2. Error message matches locked decision: `grep "djinn-server is not running. Start it with" plugin/skills/djinn-planning/new-project/SKILL.md` matches
3. Hooks unchanged: `grep "ensure-daemon" plugin/hooks/hooks.json` still matches (not removed or modified)
4. Skills mirror is complete: `diff -rq plugin/skills/ packages/planning/skills/` shows no differences
5. File count matches: `find plugin/skills/ -type f | wc -l` equals `find packages/planning/skills/ -type f | wc -l`
6. No per-runtime SKILL.md variants: one copy of each SKILL.md serves all runtimes
7. Line budgets preserved: `wc -l plugin/skills/djinn-planning/new-project/SKILL.md plugin/skills/djinn-planning/plan-milestone/SKILL.md plugin/skills/djinn-planning/discuss-milestone/SKILL.md` all under 600 lines
</verification>

<success_criteria>
The Djinn planning system has runtime-agnostic daemon health checks and a complete distributable skill bundle:
1. Every workflow SKILL.md has a "Before You Begin" section that calls system_ping() and stops with a clear error if djinn-server is unavailable
2. Claude Code's hooks.json auto-start (SessionStart -> --ensure-daemon) is preserved as the primary mechanism, with SKILL.md preamble as a second safety layer
3. packages/planning/skills/ contains an exact mirror of plugin/skills/ -- the content bundle that the NPM installer distributes to all runtimes
4. No per-runtime SKILL.md variants exist -- the same content works on Claude Code, OpenCode, Gemini CLI, and Codex
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution/05-02-SUMMARY.md`
</output>
