---
phase: 04-supporting-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/skills/djinn-planning/discuss-milestone/SKILL.md
autonomous: true
requirements:
  - SUPP-01
  - SUPP-02
  - SUPP-03

must_haves:
  truths:
    - "SKILL.md Step 3 contains a complete adaptive discussion methodology with decision-driven checkpoints, thread-following heuristics, scope creep redirection, and context presentation patterns -- not a rigid dialog script"
    - "SKILL.md Step 4 contains batch ADR write logic with granularity filtering (architectural decisions -> ADR, lightweight preferences -> scope note Preferences section), ADR numbering discovery, and cross-referencing via wikilinks"
    - "SKILL.md Step 5 contains scope note validation with explicit section structure (In Scope, Out of Scope, Preferences), wikilink aggregation to all session ADRs, and a completeness check that every discussed topic is reflected in either an ADR or the scope note"
    - "All three extension point markers ([Phase 4 implements...]) are replaced with concrete workflow instructions"
    - "The complete SKILL.md stays within the 600-line prompt budget (SKAF-06)"
  artifacts:
    - path: "plugin/skills/djinn-planning/discuss-milestone/SKILL.md"
      provides: "Complete discuss-milestone workflow with all extension points filled"
      contains: "decision-driven checkpoint"
  key_links:
    - from: "plugin/skills/djinn-planning/discuss-milestone/SKILL.md"
      to: "plugin/skills/djinn-planning/cookbook/planning-templates.md"
      via: "Step 4 references cookbook for ADR template and wikilink patterns"
      pattern: "cookbook/planning-templates"
    - from: "plugin/skills/djinn-planning/discuss-milestone/SKILL.md"
      to: "plugin/skills/djinn-planning/plan-milestone/SKILL.md"
      via: "Step 5 scope note is consumed by plan-milestone Step 1 sub-step 5"
      pattern: "Milestone.*Scope"
---

<objective>
Fill all three Phase 4 extension points in the discuss-milestone SKILL.md workflow, transforming it from a scaffold with placeholder markers into a complete, executable workflow for adaptive milestone discussion with ADR and scope capture.

Purpose: This is the core deliverable of Phase 4 -- the SKILL.md IS the product. When an LLM loads this file and executes it, it must correctly guide a design discussion, capture architectural decisions as ADR memory notes, and write a scope reference note that plan-milestone will consume.

Output: A complete `plugin/skills/djinn-planning/discuss-milestone/SKILL.md` with all extension points replaced by concrete workflow instructions.
</objective>

<execution_context>
@/home/fernando/.claude/get-shit-done/workflows/execute-plan.md
@/home/fernando/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supporting-workflows/04-CONTEXT.md
@.planning/phases/04-supporting-workflows/04-RESEARCH.md
@plugin/skills/djinn-planning/discuss-milestone/SKILL.md
@plugin/skills/djinn-planning/cookbook/planning-templates.md
@plugin/skills/djinn-planning/new-project/SKILL.md
@plugin/skills/djinn-planning/plan-milestone/SKILL.md

<interfaces>
<!-- The SKILL.md scaffolding has three extension points marked with [Phase 4 implements...] -->
<!-- These markers must be replaced with inline workflow instructions following the existing style -->

Extension Point 1 (Step 3, line ~72):
  `[Phase 4 implements the full adaptive discussion methodology here, including question generation strategies, thread-following heuristics, and decision confirmation patterns]`
  → Replace with decision-driven adaptive discussion methodology

Extension Point 2 (Step 4, line ~105):
  `[Phase 4 implements ADR quality checks and cross-referencing logic here]`
  → Replace with granularity filter, numbering discovery, batch write logic, and wikilink cross-referencing

Extension Point 3 (Step 5, line ~141):
  `[Phase 4 implements scope note validation and completeness checks here]`
  → Replace with scope note structure validation, wikilink aggregation to ADRs, and completeness check

<!-- KEY USER DECISIONS (from 04-CONTEXT.md -- these are LOCKED and NON-NEGOTIABLE): -->
<!-- - Decision-driven checkpoints, NOT fixed question count -->
<!-- - All ADRs written AFTER discussion ends (batch), NOT during -->
<!-- - No confirmation step before writing ADRs -->
<!-- - ADR granularity threshold: only architectural consequences get own ADR -->
<!-- - Single scope note per milestone with In Scope/Out of Scope/Preferences -->
<!-- - Full context assembly before discussion (roadmap + requirements + research + ADRs + build_context) -->
<!-- - Existing ADRs presented with offer to revisit, NOT skipped automatically -->
<!-- - Brief context summary shown to user before topic identification -->

<!-- CLAUDE'S DISCRETION (from 04-CONTEXT.md): -->
<!-- - Exact topic generation heuristics from requirements and research -->
<!-- - How to sense when a topic thread is exhausted -->
<!-- - Whether to offer topic expansion after selected topics complete -->
<!-- - Thread-following depth within topics -->

<!-- PLAN-MILESTONE CONSUMPTION PATH (from plan-milestone SKILL.md Step 1): -->
<!-- memory_search(query="milestone {N} scope", type="reference") -->
<!-- → Title: "Milestone {N} Scope", Type: reference -->
<!-- → Tags: ["scope", "milestone-{N}", "reference"] -->
<!-- → Permalink: reference/milestone-{N}-scope -->
<!-- → Must contain ## In Scope, ## Out of Scope, ## Preferences, ## Relations -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill Step 3 (adaptive discussion methodology) extension point</name>
  <files>plugin/skills/djinn-planning/discuss-milestone/SKILL.md</files>
  <action>
Edit the existing SKILL.md to replace the Step 3 extension point marker with the complete adaptive discussion methodology. The marker to replace is:

`[Phase 4 implements the full adaptive discussion methodology here, including question generation strategies, thread-following heuristics, and decision confirmation patterns]`

Replace it with the following structured methodology content (target: 60-80 lines). Write as directive instructions ("Do X") not explanatory prose ("The reason for X is..."). Follow the same markdown style as the surrounding scaffold.

**Section 1: Context Presentation (~8-10 lines)**

Before diving into any topic, present a brief context summary to the user showing what you loaded in Step 1. Structure it as:
- The milestone's goal (one sentence from the roadmap)
- Key requirements for this milestone (list the REQ-IDs with short descriptions)
- Relevant research themes found (list titles of research notes loaded)
- Existing ADRs that touch this milestone's domain (list by title, noting they can be revisited)

Keep the summary to 10-15 lines. The purpose is to show the user what the knowledge base already contains so they do not repeat themselves.

**Section 2: Discussion Loop (~35-45 lines)**

For each topic selected by the user in Step 2, follow this pattern:

1. **Announce the topic** and present the most relevant loaded context (research findings, requirement text, existing ADR if applicable). If an existing ADR covers this topic, explicitly surface it: "ADR-{NNN} already decided {X}. Want to revisit this, or move on?"

2. **Ask focused questions** about preferences, constraints, and trade-offs. Do NOT ask generic questions ("What do you think?"). Ask specific questions:
   - "Should [feature X] use [approach A] or [approach B]? [Brief trade-off]"
   - "What happens when [edge case]?"
   - "Is [constraint] a hard requirement or flexible?"

   Question techniques (adapted from new-project workflow):
   - **Challenge vagueness:** "Good" means what exactly?
   - **Make abstract concrete:** "Walk me through someone actually using this."
   - **Surface assumptions:** "What's already decided? What's flexible?"
   - **Find edges:** "What is this NOT?"

3. **Follow threads** -- if an answer reveals a connected concern or unexpected complexity, explore it before moving on. If a tangent reveals a new decision that affects implementation, follow it. If it wanders into adjacent milestone territory, redirect per the scope guardrail (see below).

4. **Decision-driven checkpoint** -- when the user reaches a concrete decision, confirm it explicitly:
   "So we're going with [X]. Anything else on this topic, or move on?"

   Do NOT check after a fixed number of questions. The checkpoint is triggered by a decision crystallizing, not by a counter. Some topics resolve in 30 seconds ("yes, use that library"), others need 10 minutes of back-and-forth.

5. **Track decisions internally** for the batch ADR write in Step 4. For each decision, note:
   - What was decided (the choice)
   - Why (the reasoning or trade-off that led to it)
   - What it affects downstream (which requirements or implementation areas it constrains)

6. **Scope creep redirection** -- if the user suggests something outside the milestone boundary:
   "[Feature X] sounds like it belongs in a later milestone. I'll note it so it doesn't get lost. For now, let's stay on [current topic]: [return to current question]"
   Capture the deferred idea internally for inclusion in the scope note's Out of Scope section.

**Section 3: Topic Completion (~8-10 lines)**

After all selected topics are discussed:
- Briefly summarize the decisions captured across all topics
- If the discussion revealed new gray areas that were not in the original topic list, offer to explore them: "A few new areas came up during discussion: [list]. Want to dig into any of these, or are we good?"
- Let the user decide whether to explore additional topics or proceed to ADR writing
- When the user is satisfied, announce: "Discussion complete. I'll now capture the decisions and scope boundaries in Djinn memory."

**Style guidance:** Write principles and heuristics, not scripts. Use "when you notice X, do Y" patterns. Provide question technique examples (as GSD discuss-phase does), not specific questions to ask. The instructions should enable organic conversation, not mechanical Q&A.
  </action>
  <verify>
    <automated>grep -c "\[Phase 4 implements" plugin/skills/djinn-planning/discuss-milestone/SKILL.md | grep -q "^2$" && echo "PASS: Step 3 marker replaced (2 remaining for Steps 4 and 5)" || echo "FAIL: Expected exactly 2 remaining extension markers after Step 3 replacement"</automated>
  </verify>
  <done>Step 3 has a complete adaptive discussion methodology with: context presentation before topics, decision-driven checkpoints (not fixed question count), thread-following heuristics, scope creep redirection, and topic completion flow. The [Phase 4 implements...] marker for Step 3 is replaced. Methodology reads as heuristics and principles, not a rigid dialog script.</done>
</task>

<task type="auto">
  <name>Task 2: Fill Step 4 (ADR capture) and Step 5 (scope note) extension points</name>
  <files>plugin/skills/djinn-planning/discuss-milestone/SKILL.md</files>
  <action>
Continue editing SKILL.md to replace the Step 4 and Step 5 extension point markers.

**Step 4 extension point** -- Replace `[Phase 4 implements ADR quality checks and cross-referencing logic here]` with the batch ADR write logic (target: 30-40 lines):

1. **Granularity filter:** Before writing any ADRs, review all decisions captured during discussion and classify each:
   - **ADR-worthy** (write as separate ADR): decisions that constrain implementation -- library choices, data models, API contracts, naming conventions, architectural patterns. These have downstream consequences that affect how tasks will be decomposed.
   - **Preference-only** (goes to scope note Preferences section): style choices, non-binding suggestions, cosmetic preferences. These inform but do not constrain implementation.

   If in doubt about whether a decision is ADR-worthy, apply this test: "Would a different choice here change how tasks are structured or what code gets written?" If yes, it is an ADR. If no, it is a preference.

2. **ADR numbering discovery:** Before writing any new ADRs:
   - Run `memory_search(query="ADR", type="adr")` to find all existing ADRs
   - Parse the titles to find the highest ADR number (e.g., if ADR-001 and ADR-002 exist, next is ADR-003)
   - Continue the sequence for each new ADR in this session

3. **Batch write:** For each ADR-worthy decision, write it to Djinn memory using the ADR template from [cookbook/planning-templates.md](../cookbook/planning-templates.md):

   ```
   memory_write(
     type="adr",
     title="ADR-{NNN}: {decision title}",
     content="## Context\n{what prompted this decision}\n\n## Decision\n{the choice and reasoning}\n\n## Consequences\n**Positive:**\n{benefits}\n\n**Negative:**\n{trade-offs}\n\n## Relations\n- [[Roadmap]] -- Milestone {N}\n- [[{related requirement title}]]\n- [[Milestone {N} Scope]] -- scope note for this session",
     tags=["adr", "milestone-{N}"]
   )
   ```

   Do NOT ask the user to confirm each ADR before writing. Per locked decision, trust that the conversation captured intent accurately. The user can edit later via `memory_edit`.

4. **Cross-referencing:** After all ADRs are written:
   - Each ADR's Relations section must include a wikilink to `[[Milestone {N} Scope]]` (the scope note that Step 5 will create)
   - If a new ADR supersedes or relates to an existing ADR, include a wikilink: `[[ADR-{existing}: {title}]]`
   - If a new ADR directly addresses a specific requirement, include a wikilink to the requirement note: `[[{requirement note title}]]`

5. **Quality checks:**
   - Every ADR has all four sections (Context, Decision, Consequences, Relations)
   - Every ADR's title follows the `ADR-{NNN}: {Description}` format
   - No two ADRs in this session cover the same decision (deduplicate)
   - Each ADR's Consequences section has both Positive and Negative sub-sections

**Step 5 extension point** -- Replace `[Phase 4 implements scope note validation and completeness checks here]` with scope note validation logic (target: 30-40 lines):

1. **Scope note assembly:** After all ADRs are written, assemble the scope note content:
   - **In Scope:** List specific deliverables discussed and confirmed during the session. Each item should be concrete enough to be unambiguous (not "authentication" but "JWT-based session authentication with refresh token rotation").
   - **Out of Scope:** Include all deferred ideas captured during discussion, each with an explicit reason (e.g., "belongs to milestone {M}", "deferred to v2", "out of project scope entirely").
   - **Preferences:** Include all preference-only decisions from the granularity filter in Step 4. These are free-form bullet points, not structured key-value pairs.

2. **Wikilink aggregation:** The scope note's Relations section must include:
   - `[[Roadmap]] -- Milestone {N}`
   - A wikilink to each ADR created during this session: `[[ADR-{NNN}: {title}]]`
   - Wikilinks to any relevant requirement notes referenced during discussion

   This aggregation is critical -- it makes the scope note a single entry point for plan-milestone. Reading the scope note and following its wikilinks gives plan-milestone access to all decisions.

3. **Write the scope note:**
   ```
   memory_write(
     type="reference",
     title="Milestone {N} Scope",
     content="...",
     tags=["scope", "milestone-{N}", "reference"]
   )
   ```

   The title, type, and tags MUST match exactly what plan-milestone expects (see plan-milestone SKILL.md Step 1 sub-step 5).

4. **Completeness check:** After writing the scope note, verify:
   - Every topic discussed in Step 3 is reflected in at least one of: an ADR, the In Scope section, the Out of Scope section, or the Preferences section
   - The scope note's In Scope section is not empty (if it is, the discussion did not produce actionable scope boundaries -- warn the user)
   - All ADRs created in Step 4 are wikilinked from the scope note's Relations section

   If the completeness check fails, note the gap and proceed to Step 6 -- do not block the workflow. Report the gap in the Step 6 summary so the user can address it.

5. **Existing scope note handling:** If a scope note for this milestone already exists (from a prior discuss-milestone session):
   - Read the existing note with `memory_read("reference/milestone-{N}-scope")`
   - Use `memory_edit(identifier="reference/milestone-{N}-scope", operation="replace_section", section="In Scope", content="...")` to update each section
   - Append new ADR wikilinks to the Relations section rather than overwriting

**Style guidance:** Maintain the same markdown formatting and MCP-call specificity used in the existing Steps 1-2. Keep instructions directive. The file must stay within the 600-line budget per SKAF-06.
  </action>
  <verify>
    <automated>grep -c "\[Phase 4 implements" plugin/skills/djinn-planning/discuss-milestone/SKILL.md | grep -q "^0$" && echo "PASS: No extension markers remaining" || echo "FAIL: Extension markers still present"</automated>
  </verify>
  <done>Step 4 has the complete batch ADR write logic with granularity filter, numbering discovery, cross-referencing, and quality checks. Step 5 has scope note validation with section structure, wikilink aggregation, completeness check, and existing scope note handling. Zero [Phase 4 implements...] markers remain. SKILL.md stays within the 600-line budget.</done>
</task>

</tasks>

<verification>
1. Zero `[Phase 4 implements...]` markers remain in SKILL.md: `grep -c "\[Phase 4 implements" plugin/skills/djinn-planning/discuss-milestone/SKILL.md` returns 0
2. SKILL.md contains decision-driven checkpoints: `grep "decision.*crystalliz\|decision-driven\|So we're going with" plugin/skills/djinn-planning/discuss-milestone/SKILL.md` matches
3. SKILL.md contains ADR granularity filter: `grep "ADR-worthy\|Preference-only\|granularity" plugin/skills/djinn-planning/discuss-milestone/SKILL.md` matches
4. SKILL.md contains scope note completeness check: `grep "completeness check\|every topic discussed" plugin/skills/djinn-planning/discuss-milestone/SKILL.md` matches
5. SKILL.md contains existing ADR revisit pattern: `grep "Want to revisit\|already decided" plugin/skills/djinn-planning/discuss-milestone/SKILL.md` matches
6. SKILL.md contains scope creep redirection: `grep "sounds like it belongs\|later milestone\|deferred" plugin/skills/djinn-planning/discuss-milestone/SKILL.md` matches
7. SKILL.md stays within 600-line budget: `wc -l plugin/skills/djinn-planning/discuss-milestone/SKILL.md` shows < 600 lines
8. Scope note title matches plan-milestone expectation: `grep 'title="Milestone {N} Scope"' plugin/skills/djinn-planning/discuss-milestone/SKILL.md` matches
9. Scope note tags match plan-milestone expectation: `grep '"scope", "milestone-{N}", "reference"' plugin/skills/djinn-planning/discuss-milestone/SKILL.md` matches
</verification>

<success_criteria>
The discuss-milestone SKILL.md is a complete, executable workflow with zero placeholder markers. An LLM loading this file can:
1. Load all milestone context from Djinn memory (Step 1), present a brief summary (Step 3 context presentation), and identify discussion topics (Step 2)
2. Facilitate an adaptive, organic discussion with decision-driven checkpoints and scope creep redirection (Step 3) per user's locked decisions
3. Apply a granularity filter to separate ADR-worthy decisions from lightweight preferences (Step 4)
4. Batch-write ADRs with correct numbering, template structure, and wikilink cross-referencing (Step 4)
5. Write a scope reference note with In Scope/Out of Scope/Preferences that plan-milestone can discover and consume (Step 5)
6. Present a complete session summary with created artifacts and open items (Step 6)
</success_criteria>

<output>
After completion, create `.planning/phases/04-supporting-workflows/04-01-SUMMARY.md`
</output>
