//! PageRank, entry-point distance, RRF fusion, and the
//! `RepoGraphRanking` / `RankedRepoGraphNode` data types.
//!
//! This is a placeholder file — the contents will be moved in by the
//! follow-up task `our5`. For now the module exists so the parent
//! `repo_graph` module can declare `mod ranking;` without breaking
//! sibling submodules' references.
