//! `RepoDependencyGraph` — the in-memory petgraph-backed repo graph
//! data structure plus its query/lookup/neighborhood helpers.
//!
//! This is a placeholder file — the contents will be moved in by the
//! follow-up task `our5`. For now the module exists so the parent
//! `repo_graph` module can declare `mod graph;` without breaking
//! sibling submodules' references.
