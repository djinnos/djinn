//! `RepoDependencyGraphBuilder` — the builder that turns a slice of
//! `ParsedScipIndex` into a fully-populated `RepoDependencyGraph`.
//!
//! This is a placeholder file — the contents will be moved in by the
//! follow-up task `3hrr`. For now the module exists so the parent
//! `repo_graph` module can declare `mod builder;` without breaking
//! sibling submodules' references.
