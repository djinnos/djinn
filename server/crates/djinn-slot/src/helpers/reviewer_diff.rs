//! Reviewer diff context (stub).
use crate::host::SlotContext;

pub(crate) async fn build_reviewer_diff_context(
    _task_id: &str,
    _worktree_path: &str,
    _ctx: &SlotContext,
) -> Option<String> {
    None
}
