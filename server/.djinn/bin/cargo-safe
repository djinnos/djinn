#!/bin/sh
set -eu

export CARGO_TARGET_DIR="${CARGO_TARGET_DIR:-/tmp/djinn-target}"
export CARGO_HOME="${CARGO_HOME:-/tmp/djinn-cargo-home}"
mkdir -p "$CARGO_TARGET_DIR" "$CARGO_HOME"

exec cargo "$@"
