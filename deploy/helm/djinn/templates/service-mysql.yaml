{{- if .Values.mysql.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "djinn.serviceName.mysql" . }}
  namespace: {{ include "djinn.namespace" . }}
  labels:
    {{- include "djinn.mysql.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  # Headless Service so the StatefulSet Pod gets a stable DNS name.
  clusterIP: None
  selector:
    {{- include "djinn.mysql.selectorLabels" . | nindent 4 }}
  ports:
    - name: mysql
      port: {{ .Values.mysql.service.port }}
      targetPort: mysql
      protocol: TCP
{{- end -}}
