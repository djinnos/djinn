{{- if .Values.cgroupWritableRuntimeClass.enabled }}
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: djinn-cgroup-writable
handler: runc-cgroupwritable
scheduling:
  nodeSelector:
    djinn.io/cgroup-writable: "true"
{{- end }}
