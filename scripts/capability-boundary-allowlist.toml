# Capability-boundary allowlist.
#
# Each [[entries]] block grants a narrow, auditable exemption for a specific
# capability usage in a specific file.  Entries must be exact: reviewers should
# be able to see exactly why a file+matcher is exempt.
#
# Schema:
#   capability      = "git" | "http" | "k8s"
#   path            = exact file path (e.g. "server/crates/foo/src/bar.rs");
#                     broad globs like "server/crates/**" are forbidden except
#                     narrow synthetic fixture globs used by self-tests.
#   matcher         = exact substring matched by the detector (e.g. "git2::").
#   owner           = team or individual responsible for the cleanup.
#   rationale       = short justification for the exemption.
#   expires         = ISO-8601 date after which the exemption is stale; OR
#   cleanup_issue    = link to a tracking issue for the cleanup.
#
# At least one of "expires" or "cleanup_issue" must be present.


[[entries]]
capability = "git"
	path = "server/crates/djinn_capability_guard_fixture/src/allowed.rs"
	matcher = "git2::"
	owner = "team/capability-guard-self-test"
	rationale = "Synthetic allowlist fixture for the shared guard self-test."
	expires = "2099-12-31"

[[entries]]
capability = "git"
	path = "server/crates/djinn_capability_guard_fixture/src/allowed.rs"
	matcher = "git2::"
	owner = "team/capability-guard-self-test"
	rationale = "Synthetic allowlist fixture for the shared guard self-test."
	expires = "2099-12-31"

# ── Baseline git capability allowlist entries ─────────────────────────────
# These entries pre-allowlist existing git usage outside djinn-git/ so the
# guard runs cleanly today.  They will be cleaned up as crates migrate to
# djinn-git (tracked by epic fztz).

# djinn-agent-worker
[[entries]]
capability = "git"
	path = "server/crates/djinn-agent-worker/src/checkpoint_safety/tests.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/agent-worker"
	rationale = "Test helper `git()` uses sync Command for test repo setup; production checkpoint/checkpoint-safety code uses djinn-git."
	cleanup_issue = "epic/fztz"

[[entries]]
capability = "git"
	path = "server/crates/djinn-agent-worker/src/main.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/agent-worker"
	rationale = "Test helper `git()` uses sync Command for test repo setup; production main.rs git operations use djinn-git."
	cleanup_issue = "epic/fztz"

# djinn-agent
[[entries]]
capability = "git"
	path = "server/crates/djinn-agent/src/actors/slot/lifecycle/prompt_context.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/agent"
	rationale = "Pre-existing git shell-out for prompt context; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

[[entries]]
capability = "git"
	path = "server/crates/djinn-agent/tests/phase1_supervisor.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/agent"
	rationale = "Pre-existing git shell-out in supervisor tests; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

# djinn-control-plane
[[entries]]
capability = "git"
	path = "server/crates/djinn-control-plane/src/tools/memory_tools/mod.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/control-plane"
	rationale = "Pre-existing git shell-out for memory tools; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

[[entries]]
capability = "git"
	path = "server/crates/djinn-control-plane/src/tools/pr_review_tools.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/control-plane"
	rationale = "Pre-existing git shell-out for PR review tools; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

[[entries]]
capability = "git"
	path = "server/crates/djinn-control-plane/src/tools/project_tools/lifecycle_ops.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/control-plane"
	rationale = "Pre-existing git shell-out for project lifecycle ops; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

# djinn-coordinator
[[entries]]
capability = "git"
	path = "server/crates/djinn-coordinator/src/dispatch/retry.rs"
	matcher = "git2::"
	owner = "team/coordinator"
	rationale = "Pre-existing libgit2 usage for retry dispatch; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

[[entries]]
capability = "git"
	path = "server/crates/djinn-coordinator/src/tests/mod.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/coordinator"
	rationale = "Pre-existing git shell-out in coordinator tests; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

# djinn-graph
# All four previously-baseline git shell-outs in `djinn-graph` were migrated
# behind `djinn-git` owner-crate helpers (run_git_command_in / head_commit_sha
# / rev_list_count) by cgcl. The detectors no longer match these files, so
# the corresponding allowlist entries were removed to keep the allowlist in
# sync with the actual source-call baseline.

# djinn-k8s
[[entries]]
capability = "git"
	path = "server/crates/djinn-k8s/src/graph_warmer.rs"
	matcher = "tokio::process::Command::new(\"git\")"
	owner = "team/k8s"
	rationale = "Pre-existing async git shell-out for graph warmer; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

# djinn-slot
[[entries]]
capability = "git"
	path = "server/crates/djinn-slot/src/finalize_handlers_fingerprint_tests.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/slot"
	rationale = "Pre-existing git shell-out in fingerprint tests; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

[[entries]]
capability = "git"
	path = "server/crates/djinn-slot/src/reply_loop/tests.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/slot"
	rationale = "Pre-existing git shell-out in reply loop tests; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

# djinn-stack
[[entries]]
capability = "git"
	path = "server/crates/djinn-stack/src/detect.rs"
	matcher = "git2::"
	owner = "team/stack"
	rationale = "Pre-existing libgit2 usage for stack detection; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

# djinn-supervisor
[[entries]]
capability = "git"
	path = "server/crates/djinn-supervisor/src/lib.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/supervisor"
	rationale = "Pre-existing git shell-out in supervisor; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

# djinn-workspace
[[entries]]
capability = "git"
	path = "server/crates/djinn-workspace/src/workspace.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/workspace"
	rationale = "Pre-existing git shell-out for workspace operations; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

[[entries]]
capability = "git"
	path = "server/crates/djinn-workspace/tests/smoke.rs"
	matcher = "Command::new(\"git\")"
	owner = "team/workspace"
	rationale = "Pre-existing git shell-out in workspace smoke tests; to be migrated to djinn-git."
	cleanup_issue = "epic/fztz"

# ── Baseline HTTP capability allowlist entries ─────────────────────────────
# These entries pre-allowlist existing HTTP client usage outside djinn-provider/
# so the guard runs cleanly today.  They will be cleaned up as crates migrate to
# djinn-provider (tracked by epic fztz).

# server
[[entries]]
capability = "http"
	path = "server/src/server/org_sync.rs"
	matcher = "reqwest::{"
	owner = "team/server"
	rationale = "Pre-existing destructured reqwest import for org sync operations; to be migrated to djinn-provider."
	cleanup_issue = "epic/fztz"

[[entries]]
capability = "http"
	path = "server/src/server/auth.rs"
	matcher = "reqwest::Client"
	owner = "team/server"
	rationale = "Pre-existing reqwest::Client import for auth operations; to be migrated to djinn-provider."
	cleanup_issue = "epic/fztz"

[[entries]]
capability = "http"
	path = "server/src/server/github_install.rs"
	matcher = "reqwest::Client"
	owner = "team/server"
	rationale = "Pre-existing reqwest::Client import for GitHub installation; to be migrated to djinn-provider."
	cleanup_issue = "epic/fztz"

# Header-only reqwest imports (not HTTP client construction)
[[entries]]
capability = "http"
path = "server/crates/djinn-agent/src/mcp_client.rs"
matcher = "reqwest::{"
owner = "team/agent"
rationale = "Pre-existing reqwest::header destructured import for MCP client headers; not HTTP client construction."
cleanup_issue = "epic/fztz"

# ── Baseline k8s capability allowlist entries ──────────────────────────────
# These entries pre-allowlist existing Kubernetes client/object access outside
# djinn-k8s/ so the guard runs cleanly today.  They will be cleaned up as crates
# migrate to djinn-k8s (tracked by epic fztz).

# djinn-image-controller
[[entries]]
capability = "k8s"
path = "server/crates/djinn-image-controller/src/watcher.rs"
matcher = "kube::"
owner = "team/image-controller"
rationale = "Pre-existing kube client/watcher usage for image build watching; to be migrated to djinn-k8s."
cleanup_issue = "epic/fztz"

[[entries]]
capability = "k8s"
path = "server/crates/djinn-image-controller/src/watcher.rs"
matcher = "k8s_openapi"
owner = "team/image-controller"
rationale = "Pre-existing k8s_openapi type usage for image build watching; to be migrated to djinn-k8s."
cleanup_issue = "epic/fztz"

[[entries]]
capability = "k8s"
path = "server/crates/djinn-image-controller/src/controller.rs"
matcher = "kube::"
owner = "team/image-controller"
rationale = "Pre-existing kube::Client/api usage in image controller; to be migrated to djinn-k8s."
cleanup_issue = "epic/fztz"

[[entries]]
capability = "k8s"
path = "server/crates/djinn-image-controller/src/controller.rs"
matcher = "k8s_openapi"
owner = "team/image-controller"
rationale = "Pre-existing k8s_openapi type usage in image controller; to be migrated to djinn-k8s."
cleanup_issue = "epic/fztz"

[[entries]]
capability = "k8s"
path = "server/crates/djinn-image-controller/src/build_job.rs"
matcher = "k8s_openapi"
owner = "team/image-controller"
rationale = "Pre-existing k8s_openapi type usage for building image controller jobs; to be migrated to djinn-k8s."
cleanup_issue = "epic/fztz"

[[entries]]
capability = "k8s"
path = "server/crates/djinn-image-controller/tests/unit.rs"
matcher = "kube::"
owner = "team/image-controller"
rationale = "Pre-existing kube runtime watcher usage in image controller tests; to be migrated to djinn-k8s."
cleanup_issue = "epic/fztz"

[[entries]]
capability = "k8s"
path = "server/crates/djinn-image-controller/tests/unit.rs"
matcher = "k8s_openapi"
owner = "team/image-controller"
rationale = "Pre-existing k8s_openapi type usage in image controller tests; to be migrated to djinn-k8s."
cleanup_issue = "epic/fztz"

# server
[[entries]]
capability = "k8s"
path = "server/src/server/state/mod.rs"
matcher = "kube::"
owner = "team/server"
rationale = "Pre-existing kube::Client construction for image controller and graph warmer runtime wiring; the RPC token review path has moved behind djinn-k8s."
cleanup_issue = "epic/fztz"
