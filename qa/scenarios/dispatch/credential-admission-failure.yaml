version: 1
scenarios:
  - id: dispatch.credential_admission_failure
    version: 1
    enabled: true
    profiles: [smoke-ci]
    sources: [{kind: incident, id: "credential dispatch when no dedicated active note exists"}]
    primary_coverage: dispatch.claim-admission
    secondary_coverage: [provider.credentials-preflight]
    execution: {kind: cargo-package, package: djinn-coordinator}
    isolation: {database: isolated, providers: isolated, channel: isolated}
    watch_paths: [server/crates/djinn-coordinator/src/dispatch/task_dispatch.rs]
